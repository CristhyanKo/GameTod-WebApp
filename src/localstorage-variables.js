const prefix = '@GameTod'

module.exports = {
    token: `${prefix}:token`,
    avatar: `${prefix}:avatar`,
    firstName: `${prefix}:firstName`,
    secondName: `${prefix}:secondName`,
    nick: `${prefix}:nick`,
    email: `${prefix}:email`,
}